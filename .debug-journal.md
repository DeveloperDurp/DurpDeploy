# Debug Journal - deployment terminal stream closure
Started: 2026-09-11T19:00:00Z
Goal: Confirm why the frozen deploymentStream status method does not close on a real HTMX terminal status swap.

## Environment snapshot
- Runtime: Chromium via Playwright 1.61.1, Alpine 3.15.12, HTMX 2.0.10.
- Entry: isolated DurpDeploy binary on `127.0.0.1:28090`.
- Git state: shared dirty worktree with concurrent Todo 5-9 files; task scope is deployments template/test only.
- References read: Node runtime, Playwright CLI, setup, investigate, Oracle Triple, and fix methodology.

## Hypotheses
1. Event normalization is wrong. Distinguishing evidence: observed `event.type` and registered event path. If true, fix is listener modifier.
2. HTMX detail target is stale. Distinguishing evidence: `detail.target` versus `detail.elt` IDs/text after outerHTML. If true, fix is event adapter.
3. Listener placement misses bubbling. Distinguishing evidence: document/window capture sees afterSwap while Alpine close remains absent. If true, fix is owner placement.

## Failed hypothesis round counter
- Round 1: kebab listener rendered terminal status but close count remained zero.
- Round 2: camel-modified window listener rendered terminal status but close count remained zero.

## Artifacts to revert
- [ ] `.debug-journal.md` - delete after verification.
- [ ] Temporary HTMX event capture in `.omo/evidence/task-10-alpine-frontend-compliance.mjs` - remove after diagnosis.
- [ ] tmux session `task-10-alpine-server` - stop and remove.
- [ ] `/tmp/opencode/task-10-alpine` - remove after browser QA.

## Findings
- Live SSE log order is `task10-first`, `task10-second`, `task10-third`.
- The status badge reaches `succeeded`, but tracked `EventSource.close()` count remains zero.
- Synthetic HTMX owner replacement invokes Alpine destroy: one source created, one source closed, no post-destroy update.

## Final fix
- Pending runtime event-detail capture.
