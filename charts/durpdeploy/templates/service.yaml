apiVersion: v1
kind: Service
metadata:
  name: {{ include "durpdeploy.fullname" . }}
  labels:
    {{- include "durpdeploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "durpdeploy.selectorLabels" . | nindent 4 }}
